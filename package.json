{
  "name": "baldwinandco-site",
  "private": true,
  "version": "1.0.0",
  "description": "Baldwin & Co. static marketing site (keywestkate.com)",
  "scripts": {
    "check:dupes": "node scripts/check-duplicate-keys.js",
    "lint": "eslint lang.js scripts/**/*.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "pretest": "npm run check:dupes && npm run lint"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "eslint": "^8.57.0",
    "http-server": "^14.1.1"
  }
}
